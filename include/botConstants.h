#pragma once
const float F_PI = 3.14159265358979323846f;
const float RAD_TO_DEG = 180.0f / F_PI;
const float DEG_TO_RAD = F_PI / 180.0f;
const float MODULEP_MAGNITUDE = 5.65131744f; //inches
const float MODULEP1_ANGLE = 0; //radians
const float MODULEP2_ANGLE = F_PI * 4 / 3; //radians
const float MODULEP3_ANGLE = F_PI * 2 / 3; //radians
const float MAX_ANGULAR_SPEED = 800 * DEG_TO_RAD; //radians per sec